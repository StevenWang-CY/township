# Kept for pip-only users; canonical deps live in pyproject.toml
# (`pip install -e ".[dev]"` from the repo root is the recommended path).
fastapi>=0.133
uvicorn[standard]>=0.46
websockets>=16.0
anthropic[bedrock]>=0.101
boto3>=1.43
pydantic>=2.12
python-frontmatter>=1.1
aiofiles>=25.1
httpx>=0.28
python-multipart>=0.0.26
