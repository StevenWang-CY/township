"""Scenario packages bundled with Township's Python distribution.

The runtime continues to treat each child directory as data, not Python code;
this marker only ensures wheels and sdists retain the complete zero-key demos.
"""
